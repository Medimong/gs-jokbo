# 외과학 실습족보 아카이브 — 인계

이 파일 하나로 아무 맥락 없이 이어서 작업할 수 있게 적었다. 새 세션은 이 파일과
`_pipeline/AGENT_SPEC.md`, `_pipeline/CONCEPT_AGENT_SPEC.md`를 먼저 읽어라.

## 무엇을 만들고 있나
서울대 의대 외과학 실습족보(2022~2026)를 문항 단위로 정제해 오프라인 학습 사이트로 만든다.
소아과 족보 사이트(`~/Desktop/late registration/3학년/소아/소아과족보_사이트/`)가 원형이고,
그 방법론(1문항 1에이전트, 원본 대조 복원, 이미지 직접 확인)을 그대로 따르되
**해설 구조와 디자인이 다르다.**

## 원본 (읽기 전용, 절대 쓰지 마라)
`~/Library/CloudStorage/SynologyDrive-의학과3학년/외과학/4-1. 실습족보/{2022..2026}/`
Synology 동기화 폴더라 여기에 쓰면 서버로 올라간다. 읽기만 한다.

## 사용자가 정한 것
- 범위는 2022~2026. **출제 3회 이상 188문항**만 작업한다(전체 666개 중).
- 수치만 바뀐 변형(같은 문제, 체중만 다름)은 **한 문항으로 두고** `compare_note`에서 다룬다.
- 원본 시험지 자체의 오류는 **고치지 않고** `source_notes`에 기록만 한다.
- 의학용어와 해부 구조물은 **한글과 영어를 병기**한다.
- 화살표, em dash, 하이픈 두 개, 글머리 이모지를 쓰지 않는다.
- **모든 문항에 선지별 검토를 붙이지 않는다.** 문항 유형에 따라 해설 구조가 다르다.
  가이드라인은 정리 카드의 표로 올리고 문항 해설은 "이 환자는 이 칸"으로 끝낸다.
  계산형은 공식과 검산만. 자세한 것은 AGENT_SPEC 5단계.

## 파이프라인 (순서대로)
```
python3 _pipeline/a_inventory.py      # 소스 31개 확정 -> a0_files.json
python3 _pipeline/c_collect.py        # 전 소스 파싱 -> c_pool.json, images/ (2746문항, 1019장)
python3 _pipeline/d_dupcand.py        # 중복 후보 탐지 -> d_dupcand.json (병합은 하지 않는다)
python3 _pipeline/f_targets.py        # 작업 대상 확정 -> f_targets.json
python3 _pipeline/e_extract.py <qid>  # 문항의 모든 복원 버전 묶기 -> _pipeline/extract/q{id}.json
  (여기서 에이전트가 1문항씩 작업해 _pipeline/out/q{id}.json 산출)
python3 _pipeline/fix_style.py        # 금지 문장부호 수리
node _pipeline/apply_out.js           # 검증 후 questions/{분과}/q{id}.json 적립
node _pipeline/link_concepts.js       # 에이전트가 제안한 개념 이름을 실제 카드 slug에 연결
node _pipeline/check_all.js           # 무결성 점검 (이미지, 카드 링크, 스키마, 문장부호)
node _pipeline/build.js               # data.js 번들 생성
```
사이트는 `index.html`을 더블클릭하면 열린다(오프라인 동작).

## 에이전트 운용
- **문항 1개 = 에이전트 1개.** 묶어서 시키면 할루시네이션이 생긴다(소아과에서 겪음).
- 모델은 sonnet으로 충분하다. 동시 10개 정도까지 돌린다.
- 프롬프트는 `_pipeline/AGENT_SPEC.md`를 읽으라고 지시하고, 그 문항의 알려진 특징만 덧붙인다.
- 정리 카드는 `_pipeline/CONCEPT_AGENT_SPEC.md`를 읽게 한다. 카드에는 **결정표 또는
  `<ol class="flow">` 단계별 프로토콜이 반드시** 들어가야 한다.
- 배치가 끝나면 에이전트들이 낸 `new_concepts`를 모아 중복을 합친 뒤 카드를 만든다.
  카드 하나에 여러 문항이 붙는 것이 정상이고, 그게 이 구조의 목적이다.

## 지금까지 겪은 함정
- **분과 라벨 오염**: 2016년 이전 대합본이 주제 헤딩(`[해부학]`)을 분과로 물려받아
  '총론'이 부풀어 있다. 대상 180개 중 49개가 '총론'인데 대부분 실제로는 다른 분과다.
  에이전트가 내용을 보고 교정한다(AGENT_SPEC에 규칙 있음).
- **이미지 중복**: 한 문항에 6~14장이 붙는데 실제로 쓸 건 1~3장이다. 같은 그림의
  리사이즈본이라 md5가 달라 자동 제거가 안 된다. 에이전트가 직접 열어 봐야 걸러진다.
- **TIFF와 EMF**: 브라우저가 못 읽는다. `c_collect.py`가 저장 즉시 PNG로 변환한다.
- **계산식 오류**: GRWR을 `720 ÷ 72 × 100`으로 적어 1000이 나온 적이 있다. 계산형은
  반드시 검산해라.
- **파서가 정답을 놓치는 경우**: q212는 원본에 `정답) 4`가 있는데 추출본에는 없었다.
  에이전트가 원본을 직접 열게 하는 이유다.
- **빈 선지를 버리면 정답이 어긋난다**(q1527에서 발견): 복원자가 선지 하나를 빈칸으로 남긴
  경우 파서가 그것을 버리고 나머지를 재번호한다. 정답 번호는 선지 위치에 의존하므로 중간
  선지가 사라지면 정답이 다른 선지를 가리키게 된다. 버려진 번호 글리프가 발문 끝에 흘러드는
  것도 같은 원인이다. **파서 개선 항목**: 빈 선지를 자리표시자로 유지할 것.
- **적립 사고 두 번**: 산출물 인박스(`_pipeline/out/`)의 옛 파일이 정본(`questions/`)을
  덮어써 재작업이 날아갔다. 지금은 적립이 끝난 산출물을 `out/applied/`로 옮겨 재적립 자체를
  막고, `fix_style.py`는 내용이 바뀔 때만 파일을 쓴다. 배치 전 스냅샷도 남긴다.
- **그룹 단위 완료 판정**: 같은 중복 그룹인데 대표 qid가 달라 이미 끝낸 문항을 다시
  잡는 일이 있었다. `f_targets.py`가 그룹 단위로 판정한다.

## 진행 상황
- 파일럿 10문항 완료(분과별 1개씩). 사용자 검토를 받아 해설 구조를 유형별로 개정했다.
- 정리 카드 10개 완성(합계 약 69,000자, 판단표 20개, 단계별 프로토콜 5개).
- **남은 작업: 180문항.** 목록은 `_pipeline/f_targets.json`, `done: false`인 것들.
  출제 횟수 내림차순으로 정렬되어 있다.
